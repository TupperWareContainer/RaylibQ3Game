#pragma once
#include <iostream>
#include <raylib.h>
#include <raymath.hpp>
using namespace std; 
class UIButton {
public:
	UIButton(string,int,int);
	void setPos(float, float); 
	void drawButton(bool); 
//private: 
	
};
